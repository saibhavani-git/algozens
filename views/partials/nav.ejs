<nav class="navbar sticky-top navbar-expand-lg navbar-dark app-nav">
    <div class="container-xl">
        <a class="navbar-brand" href="/">
            <span class="brand-mark">A</span>
            Algozens
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav me-auto">
                <a class="nav-link" href="/">Home</a>
                <a class="nav-link" href="/dsa">Practice</a>
                <% if (currentUser) { %>
                    <a class="nav-link" href="/profile">Dashboard</a>
                <% } %>
            </div>
            <div class="navbar-nav align-items-lg-center">
                <% if (!currentUser) { %>
                    <a class="nav-link" href="/login">Login</a>
                    <a class="nav-link" href="/register">Register</a>
                <% } else { %>
                    <a class="nav-link profile-nav" href="/profile">
                        <span class="nav-avatar"><%= currentUser.username.slice(0, 1).toUpperCase() %></span>
                        <%= currentUser.username %>
                    </a>
                    <a class="nav-link" href="/logout">Logout</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>
